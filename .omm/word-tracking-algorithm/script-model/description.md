脚本数据模型。将原始文本解析为结构化的词级和句级表示：
- ScriptToken：每个词的原始文本、归一化形式、Metaphone 编码、字符偏移量
- Sentence：句级结构，含原始文本、显示文本、可选的 Markdown 标题注解
- 预处理管线：Markdown 剥离 → 空白分词 → 归一化 → Metaphone 编码 → 句子边界检测
- 句子分割基于 `.` `?` `!` 加空白，双换行作为段落分隔
