模糊匹配引擎。多层级容错比较，从精确到宽松依次尝试：
1. 精确匹配：a == b
2. 语音匹配：Double Metaphone 编码相同（处理同音词如 right/write）
3. 前缀匹配：一方是另一方的前缀，且较短词长度 >= 较长词的一半
4. 子串匹配：长度 >= 4 时，一方包含另一方
5. 共同前缀：共享前缀长度 >= max(2, 较短词长度的 60%)
6. 编辑距离：
   - 短词（<=4）：允许 1 次编辑
   - 中词（<=8）：允许 2 次编辑
   - 长词：允许 max(lenA, lenB) / 3 次编辑
