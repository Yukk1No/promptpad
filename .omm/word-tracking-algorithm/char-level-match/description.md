字符级模糊匹配。逐字符对齐语音转录和脚本文本，跳过非字母数字字符。核心逻辑：
- 双指针扫描：si（源文本指针）和 ri（识别文本指针）同步前进
- 字符相同时双指针前进
- 不匹配时尝试在识别文本中前跳最多 3 个字符寻找同步点
- 仍不匹配则在源文本中前跳最多 3 个字符
- 都失败则两个指针各前进 1 步
- 扫描窗口限制 500 字符防止性能退化
- 返回最后成功匹配字符在源文本中的偏移量
