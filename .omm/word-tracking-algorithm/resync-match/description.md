灾难恢复重同步（Resync Match）。当追踪完全丢失时的最后手段：
- 触发条件：连续 3 次无进展（staleCount >= staleThreshold）且收到 final 结果
- 在当前句子之后的 5 个句子范围内搜索
- 对每个候选句子，以其起始偏移量运行字符级和词级匹配
- 选择得分最高的句子，要求至少匹配 5 个字符
- 成功后直接跳转到该句子位置并重置 stale 计数
- 风险：可能在用户故意停顿时误触发跳转
